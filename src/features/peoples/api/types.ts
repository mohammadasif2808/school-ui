export interface Student {
  id: string | number;
  name: string;
  class: string;
  admissionNo: string;
  contact: string;
}

export interface Staff {
  id: string | number;
  name: string;
  role: string;
  department: string;
  contact: string;
}
